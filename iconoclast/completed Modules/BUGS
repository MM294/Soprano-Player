on load the playmark exists if theres stuff to load
Folders with identical names will break the combo box
add radio station dialog sometimes fails with "AttributeError: IconoRadio instance has no attribute 'editPref'"

##Not Bugs Below just Notes##

from gi.repository import Gtk

# You must import GdkX11 for the window.get_xid() method to be present:
from gi.repository import GdkX11

window = Gtk.Window()
drawing = Gtk.DrawingArea()
window.add(drawing)

# Before show_all(), drawing.get_property('window') is None:
window.show_all()
xid = drawing.get_property('window').get_xid()
print('xid =', xid)
